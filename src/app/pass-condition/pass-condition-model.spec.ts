import { PassConditionModel } from './pass-condition-model';

describe('PassConditionModel', () => {
  it('should create an instance', () => {
    expect(new PassConditionModel()).toBeTruthy();
  });
});
